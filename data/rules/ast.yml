rules:
  - name: From AST
    id: ast.suspicious-variable
    pattern: |-
      (?s)(.+)
    min_entropy: 0.1
    confidence: high
    examples:
      - password      = "123456"

# rules:
#   - name: From AST
#     id: ast.suspicious-variable
#     pattern: |
#       (?s)^(.*)$
#     min_entropy: 0.1
#     confidence: high
